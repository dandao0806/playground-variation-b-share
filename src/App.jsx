// Variation B: renders the Phase 1 auto-policy layout as the sole page.
// The wider multi-page playground still lives in the main repo — this
// share build is single-page so testers land directly on the prototype.
import Phase1Page from './pages/Phase1Page.jsx'

export default function App() {
  return <Phase1Page />
}
